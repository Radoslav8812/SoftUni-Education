Create DATABASE [NormalForms]

-- 1) Every case must have only 1(single value) value inside.
-- 2) Each column must have same data type (domain).
-- 3) Each column must have own unique name.
-- 4) The order of the data doesn't matter.
-- 5) Correct dependency.
-- 6) Every table must have 5 normal form rules and every table/relation. Must have PK and 1 more column (attribure) also.